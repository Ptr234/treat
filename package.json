{
  "name": "onestopcentre-uganda-monorepo",
  "version": "1.1.0",
  "description": "OneStopCentre Uganda - Complete full-stack application with React frontend and Node.js backend",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend --legacy-peer-deps && npm install --prefix backend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "lint": "npm run lint --prefix frontend",
    "lint:fix": "npm run lint:fix --prefix frontend",
    "test": "npm test --prefix frontend",
    "clean": "rm -rf frontend/dist frontend/node_modules backend/node_modules node_modules",
    "deploy:frontend": "npm run build:frontend && npm run deploy --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "react",
    "nodejs",
    "express",
    "postgresql",
    "uganda",
    "government-services",
    "monorepo"
  ],
  "author": "Uganda Investment One Stop Center <support@onestopcentre.ug>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ptr234/treat.git"
  },
  "bugs": {
    "url": "https://github.com/Ptr234/treat/issues"
  },
  "homepage": "https://oscdigitaltool.com",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}